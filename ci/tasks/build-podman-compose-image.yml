---
platform: linux

image_resource:
    type: registry-image
    source:
        repository: 192.168.88.20:5000/oci-build-task
        tag: master-arm64
        insecure: true

inputs:
- name: ghost-cloudflare-r2

outputs:
- name: image

params:
    CONTEXT: ghost-cloudflare-r2/ci/images/podman-compose
    UNPACK_ROOTFS: true

caches:
- path: cache

run:
#   path: build
  path: build
#   args: ["build", "-f", "ghost-cloudflare-r2/ci/images/podman-compose/Dockerfile", "-t", "podman-compose", "ghost-cloudflare-r2/ci/images/podman-compose"]
    # path: ls
#   args: ["-al"]
  # dir: ghost-cloudflare-r2
  # path: id
  # args: ["run", "--rm", "alpine", "echo", "hello"]
  # args: ["ps"]
