---
platform: linux

# image: podman-compose-image

image_resource:
  type: registry-image
  source:
    repository: 192.168.88.20:5000/podman-compose
    tag: latest

inputs:
- name: ghost-cloudflare-r2
- name: minio-image
- name: ghost-image
- name: hurl-image
- name: podman-compose-image

run:
  path: ghost-cloudflare-r2/ci/scripts/run-integration-tests.sh
  # path: ls
  # args: ["-al", "minio-image"]
  # dir: ghost-cloudflare-r2
  # path: id
  # args: ["run", "--rm", "alpine", "echo", "hello"]
  # args: ["ps"]
